<%- include('../partials/header', { title: 'Rescue Flare - Puls8' }) %>
  <%- include('../partials/navbar') %>

    <div class="container mt-4">
      <h2 class="mb-4 text-danger">Deploy Rescue Flare</h2>

      <!-- 
    TODO: BUILD THE RESCUE FLARE FORM
    1. Create a form with `action="/student/rescue"` and `method="POST"`.
    2. Add a `<select>` dropdown.
    3. Loop over `tasks` and create an `<option>` for each one.
       Set the `value` attribute of the option to the task's ID!
       e.g., `<option value="<%%= task.id %%>">`
    4. Add a submit button to fire the flare!
    
    TIP: Open `views/master.ejs` to copy-paste the exact HTML structure for Forms!
  -->
      <p class="text-muted border p-3">TODO: Build out the form to ping an instructor via Discord</p>

      <%- include('../partials/debugData') %>
    </div>

    <%- include('../partials/footer') %>