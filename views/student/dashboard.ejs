<%- include('../partials/header', { title: 'Student Dashboard - Puls8' }) %>
  <%- include('../partials/navbar') %>

    <div class="container mt-4">
      <h2 class="mb-4">Student Dashboard</h2>

      <!-- 
    TODO: BUILD THE STUDENT SCOREBOARD
    1. Check if `studentData` and `studentData.Scores` exists.
    2. Loop over the scores and render a table showing the Task Name, Weight, and Score.
    3. You can even calculate the total average or KKM passing grade here!
    
    TIP: Open `views/master.ejs` to copy-paste the exact HTML structure for Tables!
  -->
      <p class="text-muted border p-3">TODO: Build out the Student view using studentData</p>

      <a href="/student/rescue" class="btn btn-warning mt-3">Deploy Rescue Flare</a>

      <%- include('../partials/debugData') %>
    </div>

    <%- include('../partials/footer') %>