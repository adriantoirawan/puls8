<nav class="navbar navbar-dark bg-dark mb-4">
  <div class="container">
    <a class="navbar-brand" href="/">Puls8</a>
    <div>
      <% if (typeof role !== 'undefined') { %>
        <% if (role === 'instructor') { %>
          <a class="btn btn-outline-light me-2" href="/instructor">Dashboard</a>
        <% } else if (role === 'student') { %>
          <a class="btn btn-outline-light me-2" href="/student">Dashboard</a>
        <% } %>
      <% } %>
      <a class="btn btn-danger" href="/logout">Logout</a>
    </div>
  </div>
</nav>