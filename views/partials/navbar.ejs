<nav class="navbar navbar-dark bg-dark mb-4">
  <div class="container">
    <a class="navbar-brand" href="/">Puls8</a>
    <div>
      <!-- 
          TODO: DYNAMIC LINK FOR DASHBOARD
          1. The `href` below should point to either `/instructor or /student` depending on the user role
          2. Use EJS conditional logic (e.g., `<%% if (role === 'instructor') { %%>`) to render a link to `/instructor` or `/student` instead.
          3. (Hint: Ensure you are passing `role` from `req.session` to your views in the controllers!)
        -->
      <a class="btn btn-outline-light me-2" href="#">Dashboard</a>
      <a class="btn btn-danger" href="/logout">Logout</a>
    </div>
  </div>
</nav>