<%- include('../partials/header', { title: 'Add Class - Puls8' }) %>
<%- include('../partials/navbar') %>

<div class="container mt-4" style="max-width: 600px;">
  <h2 class="mb-4">Create New Class</h2>
  <%- include('../partials/errorAlert') %>

  <div class="card shadow-sm">
    <div class="card-body">
      <form action="/instructor/classes/add" method="POST">
        
        <div class="mb-3">
          <label class="form-label fw-bold">Class Name</label>
          <input type="text" class="form-control" name="name" placeholder="e.g. Fox" required>
        </div>

        <div class="mb-3">
          <label class="form-label fw-bold">Phase Level</label>
          <input type="number" class="form-control" name="phaseLevel" min="0" max="3" placeholder="e.g. 1" required>
        </div>

        <div class="d-flex gap-2">
           <button type="submit" class="btn btn-primary w-100">Create Class</button>
           <a href="/instructor" class="btn btn-secondary w-100">Cancel</a>
        </div>
      </form>
    </div>
  </div>
</div>

<%- include('../partials/footer') %>
